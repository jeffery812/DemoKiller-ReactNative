/**
 * Sample React Native App
 * https://github.com/facebook/react-native
 * @flow
 */

import {
  AppRegistry,
} from 'react-native';

import AppNavigator from './application/App';

AppRegistry.registerComponent('DemoKiller', () => AppNavigator);
